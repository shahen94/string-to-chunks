{
  "name": "string-to-chunks",
  "version": "1.0.1",
  "description": "Generate chunks of strings",
  "main": "index.js",
  "typings": "./build/string-to-chunks.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "prepublish": "yarn build",
    "build": "del ./build && tsc"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/shahen94/string-to-chunks"
  },
  "files": [
    "build"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.5",
    "typescript": "^4.2.4",
    "del-cli": "^3.0.1"
  }
}
